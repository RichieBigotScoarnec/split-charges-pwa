{
  "rules": {
    ".read": false,
    ".write": false,
    "household": {
      ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com')",
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com') && newData.exists()",
      "salaries": {
        "vous": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "conjointe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "extraVous": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "extraConjointe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "members": {
        "vous": {
          ".validate": "newData.isString() && newData.val().length <= 60"
        },
        "conjointe": {
          ".validate": "newData.isString() && newData.val().length <= 60"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "shareMode": {
        "mode": {
          ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
        },
        "customPercents": {
          "mode": {
            ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
          },
          "vous": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
          },
          "conjointe": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
          },
          "$autre": {
            ".validate": false
          },
          ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "carryOverEnabled": {
        ".validate": "newData.isBoolean()"
      },
      "categoryBudgets": {
        "$categorie": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10000000"
        },
        ".validate": "newData.hasChildren()"
      },
      "customCategories": {
        "$rang": {
          ".validate": "newData.isString() ? newData.val().length <= 100 : newData.hasChildren()",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "color": {
            ".validate": "newData.isString() && newData.val().length <= 30"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "customDestinations": {
        "$rang": {
          ".validate": "newData.isString() ? newData.val().length <= 100 : newData.hasChildren()",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "color": {
            ".validate": "newData.isString() && newData.val().length <= 30"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "envelopes": {
        "$rang": {
          ".validate": "newData.hasChildren(['id', 'label']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.hasChild('proprietaire')) && (!newData.hasChild('proprietaire') || newData.child('perimetre').val() === 'solo')",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "budget": {
            ".validate": "newData.isNumber() && newData.val() > 0 && newData.val() <= 10000000"
          },
          "debut": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)"
          },
          "fin": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)"
          },
          "cloturee": {
            ".validate": "newData.isBoolean()"
          },
          "nature": {
            ".validate": "newData.isString() && (newData.val() === 'mensuelle' || newData.val() === 'cagnotte')"
          },
          "report": {
            ".validate": "newData.isBoolean()"
          },
          "rang": {
            ".validate": "newData.isString() && (newData.val() === 'fixe' || newData.val() === 'mensuel' || newData.val() === 'provision' || newData.val() === 'epargne' || newData.val() === 'reserve')"
          },
          "perimetre": {
            ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
          },
          "proprietaire": {
            ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
          },
          "creePar": {
            ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
          },
          "creeLe": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
          },
          "theme": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "versements": {
        "$enveloppe": {
          "$versement": {
            "montant": {
              ".validate": "newData.isNumber() && newData.val() > 0 && newData.val() <= 10000000"
            },
            "date": {
              ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
            },
            "auteur": {
              ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
            },
            "timestamp": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
            },
            "deleted": {
              ".validate": "newData.isBoolean()"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren(['montant', 'auteur'])"
          }
        }
      },
      "reminders": {
        "finMois": {
          ".validate": "newData.isBoolean()"
        },
        "budget": {
          ".validate": "newData.isBoolean()"
        },
        "budgetAmount": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10000000"
        },
        "reimbursement": {
          ".validate": "newData.isBoolean()"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "periods": {
        "$periode": {
          ".validate": "$periode.matches(/^[0-9]{4}-(0[1-9]|1[0-2])$/) && newData.hasChildren()",
          "salaries": {
            "vous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "conjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "extraVous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "extraConjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren()"
          },
          "fixedCharges": {
            "$id": {
              "description": {
                ".validate": "newData.isString() && newData.val().length <= 200"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "category": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryId": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryIcon": {
                ".validate": "newData.isString() && newData.val().length <= 20"
              },
              "paidBy": {
                ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe' || newData.val() === 'partage' || newData.val() === 'both')"
              },
              "perimetre": {
                ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
              },
              "destination": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "envelope": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "recurring": {
                ".validate": "newData.isBoolean()"
              },
              "splitMode": {
                ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
              },
              "splitOverride": {
                "mode": {
                  ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
                },
                "vous": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "conjointe": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "heure": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^([01][0-9]|2[0-3]):[0-5][0-9]$/))"
              },
              "location": {
                "lat": {
                  ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90"
                },
                "lng": {
                  ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180"
                },
                "name": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "$autre": {
                  ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
                }
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.child('paidBy').val() === 'vous' || newData.child('paidBy').val() === 'conjointe')"
            },
            ".validate": "newData.hasChildren()"
          },
          "variableCharges": {
            "$id": {
              "description": {
                ".validate": "newData.isString() && newData.val().length <= 200"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "category": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryId": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryIcon": {
                ".validate": "newData.isString() && newData.val().length <= 20"
              },
              "paidBy": {
                ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe' || newData.val() === 'partage' || newData.val() === 'both')"
              },
              "perimetre": {
                ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
              },
              "destination": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "envelope": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "recurring": {
                ".validate": "newData.isBoolean()"
              },
              "splitMode": {
                ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
              },
              "splitOverride": {
                "mode": {
                  ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
                },
                "vous": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "conjointe": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "heure": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^([01][0-9]|2[0-3]):[0-5][0-9]$/))"
              },
              "location": {
                "lat": {
                  ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90"
                },
                "lng": {
                  ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180"
                },
                "name": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "$autre": {
                  ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
                }
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.child('paidBy').val() === 'vous' || newData.child('paidBy').val() === 'conjointe')"
            },
            ".validate": "newData.hasChildren()"
          },
          "reimbursements": {
            "$id": {
              "direction": {
                ".validate": "newData.isString() && (newData.val() === 'vous-to-conjointe' || newData.val() === 'conjointe-to-vous')"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "note": {
                ".validate": "newData.isString() && newData.val().length <= 1000"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount', 'direction'])"
            },
            ".validate": "newData.hasChildren()"
          },
          "reconductedFrom": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}$/)"
          },
          "shareMode": {
            ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
          },
          "customPercents": {
            "vous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
            },
            "conjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren(['vous', 'conjointe']) && newData.child('vous').val() + newData.child('conjointe').val() === 100"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "$autre": {
        ".validate": false
      },
      ".validate": "newData.hasChildren()"
    },
    "sandbox": {
      ".read": "auth != null && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com' || auth.token.email === 'testfairsplit@gmail.com')",
      ".write": "auth != null && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com' || auth.token.email === 'testfairsplit@gmail.com')",
      "salaries": {
        "vous": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "conjointe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "extraVous": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "extraConjointe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "members": {
        "vous": {
          ".validate": "newData.isString() && newData.val().length <= 60"
        },
        "conjointe": {
          ".validate": "newData.isString() && newData.val().length <= 60"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "shareMode": {
        "mode": {
          ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
        },
        "customPercents": {
          "mode": {
            ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
          },
          "vous": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
          },
          "conjointe": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
          },
          "$autre": {
            ".validate": false
          },
          ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "carryOverEnabled": {
        ".validate": "newData.isBoolean()"
      },
      "categoryBudgets": {
        "$categorie": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10000000"
        },
        ".validate": "newData.hasChildren()"
      },
      "customCategories": {
        "$rang": {
          ".validate": "newData.isString() ? newData.val().length <= 100 : newData.hasChildren()",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "color": {
            ".validate": "newData.isString() && newData.val().length <= 30"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "customDestinations": {
        "$rang": {
          ".validate": "newData.isString() ? newData.val().length <= 100 : newData.hasChildren()",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "color": {
            ".validate": "newData.isString() && newData.val().length <= 30"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "envelopes": {
        "$rang": {
          ".validate": "newData.hasChildren(['id', 'label']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.hasChild('proprietaire')) && (!newData.hasChild('proprietaire') || newData.child('perimetre').val() === 'solo')",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "label": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "icon": {
            ".validate": "newData.isString() && newData.val().length <= 20"
          },
          "budget": {
            ".validate": "newData.isNumber() && newData.val() > 0 && newData.val() <= 10000000"
          },
          "debut": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)"
          },
          "fin": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)"
          },
          "cloturee": {
            ".validate": "newData.isBoolean()"
          },
          "nature": {
            ".validate": "newData.isString() && (newData.val() === 'mensuelle' || newData.val() === 'cagnotte')"
          },
          "report": {
            ".validate": "newData.isBoolean()"
          },
          "rang": {
            ".validate": "newData.isString() && (newData.val() === 'fixe' || newData.val() === 'mensuel' || newData.val() === 'provision' || newData.val() === 'epargne' || newData.val() === 'reserve')"
          },
          "perimetre": {
            ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
          },
          "proprietaire": {
            ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
          },
          "creePar": {
            ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
          },
          "creeLe": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
          },
          "theme": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "versements": {
        "$enveloppe": {
          "$versement": {
            "montant": {
              ".validate": "newData.isNumber() && newData.val() > 0 && newData.val() <= 10000000"
            },
            "date": {
              ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
            },
            "auteur": {
              ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe')"
            },
            "timestamp": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
            },
            "deleted": {
              ".validate": "newData.isBoolean()"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren(['montant', 'auteur'])"
          }
        }
      },
      "reminders": {
        "finMois": {
          ".validate": "newData.isBoolean()"
        },
        "budget": {
          ".validate": "newData.isBoolean()"
        },
        "budgetAmount": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10000000"
        },
        "reimbursement": {
          ".validate": "newData.isBoolean()"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "newData.hasChildren()"
      },
      "periods": {
        "$periode": {
          ".validate": "$periode.matches(/^[0-9]{4}-(0[1-9]|1[0-2])$/) && newData.hasChildren()",
          "salaries": {
            "vous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "conjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "extraVous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "extraConjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren()"
          },
          "fixedCharges": {
            "$id": {
              "description": {
                ".validate": "newData.isString() && newData.val().length <= 200"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "category": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryId": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryIcon": {
                ".validate": "newData.isString() && newData.val().length <= 20"
              },
              "paidBy": {
                ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe' || newData.val() === 'partage' || newData.val() === 'both')"
              },
              "perimetre": {
                ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
              },
              "destination": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "envelope": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "recurring": {
                ".validate": "newData.isBoolean()"
              },
              "splitMode": {
                ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
              },
              "splitOverride": {
                "mode": {
                  ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
                },
                "vous": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "conjointe": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "heure": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^([01][0-9]|2[0-3]):[0-5][0-9]$/))"
              },
              "location": {
                "lat": {
                  ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90"
                },
                "lng": {
                  ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180"
                },
                "name": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "$autre": {
                  ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
                }
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.child('paidBy').val() === 'vous' || newData.child('paidBy').val() === 'conjointe')"
            },
            ".validate": "newData.hasChildren()"
          },
          "variableCharges": {
            "$id": {
              "description": {
                ".validate": "newData.isString() && newData.val().length <= 200"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "category": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryId": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "categoryIcon": {
                ".validate": "newData.isString() && newData.val().length <= 20"
              },
              "paidBy": {
                ".validate": "newData.isString() && (newData.val() === 'vous' || newData.val() === 'conjointe' || newData.val() === 'partage' || newData.val() === 'both')"
              },
              "perimetre": {
                ".validate": "newData.isString() && (newData.val() === 'commun' || newData.val() === 'solo')"
              },
              "destination": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "envelope": {
                ".validate": "newData.isString() && newData.val().length <= 100"
              },
              "recurring": {
                ".validate": "newData.isBoolean()"
              },
              "splitMode": {
                ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
              },
              "splitOverride": {
                "mode": {
                  ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
                },
                "vous": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "conjointe": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "!newData.hasChildren(['vous', 'conjointe']) || newData.child('vous').val() + newData.child('conjointe').val() === 100"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "heure": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^([01][0-9]|2[0-3]):[0-5][0-9]$/))"
              },
              "location": {
                "lat": {
                  ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90"
                },
                "lng": {
                  ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180"
                },
                "name": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "$autre": {
                  ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
                }
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount']) && (!newData.hasChild('perimetre') || newData.child('perimetre').val() !== 'solo' || newData.child('paidBy').val() === 'vous' || newData.child('paidBy').val() === 'conjointe')"
            },
            ".validate": "newData.hasChildren()"
          },
          "reimbursements": {
            "$id": {
              "direction": {
                ".validate": "newData.isString() && (newData.val() === 'vous-to-conjointe' || newData.val() === 'conjointe-to-vous')"
              },
              "amount": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
              },
              "note": {
                ".validate": "newData.isString() && newData.val().length <= 1000"
              },
              "date": {
                ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
              },
              "timestamp": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
              },
              "deleted": {
                ".validate": "newData.isBoolean()"
              },
              "$autre": {
                ".validate": "newData.isBoolean() || newData.isNumber() || (newData.isString() && newData.val().length <= 500)"
              },
              ".validate": "newData.hasChildren(['amount', 'direction'])"
            },
            ".validate": "newData.hasChildren()"
          },
          "reconductedFrom": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}$/)"
          },
          "shareMode": {
            ".validate": "newData.isString() && (newData.val() === 'prorata' || newData.val() === '50-50' || newData.val() === 'custom')"
          },
          "customPercents": {
            "vous": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
            },
            "conjointe": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
            },
            "$autre": {
              ".validate": false
            },
            ".validate": "newData.hasChildren(['vous', 'conjointe']) && newData.child('vous').val() + newData.child('conjointe').val() === 100"
          },
          "$autre": {
            ".validate": false
          }
        },
        ".validate": "newData.hasChildren()"
      },
      "$autre": {
        ".validate": false
      },
      ".validate": "newData.hasChildren()"
    },
    "prive": {
      "vous": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || (auth.token.email === 'cindypepe.cp95@gmail.com' && root.child('aval/vous/actif').val() === true))",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'bigot.richard@gmail.com'",
        "periods": {
          "$periode": {
            "depenses": {
              "$id": {
                "montant": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
                },
                "description": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "category": {
                  ".validate": "newData.isString() && newData.val().length <= 100"
                },
                "date": {
                  ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "deleted": {
                  ".validate": "newData.isBoolean()"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "newData.hasChildren(['montant'])"
              }
            },
            "$autre": {
              ".validate": false
            }
          }
        },
        "$autre": {
          ".validate": false
        }
      },
      "conjointe": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'cindypepe.cp95@gmail.com' || (auth.token.email === 'bigot.richard@gmail.com' && root.child('aval/conjointe/actif').val() === true))",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'cindypepe.cp95@gmail.com'",
        "periods": {
          "$periode": {
            "depenses": {
              "$id": {
                "montant": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
                },
                "description": {
                  ".validate": "newData.isString() && newData.val().length <= 200"
                },
                "category": {
                  ".validate": "newData.isString() && newData.val().length <= 100"
                },
                "date": {
                  ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/))"
                },
                "timestamp": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
                },
                "deleted": {
                  ".validate": "newData.isBoolean()"
                },
                "$autre": {
                  ".validate": false
                },
                ".validate": "newData.hasChildren(['montant'])"
              }
            },
            "$autre": {
              ".validate": false
            }
          }
        },
        "$autre": {
          ".validate": false
        }
      }
    },
    "aval": {
      "vous": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com')",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'bigot.richard@gmail.com'",
        "actif": {
          ".validate": "newData.isBoolean()"
        },
        "publieLeTotal": {
          ".validate": "newData.isBoolean()"
        },
        "accordeLe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
        },
        "accordePar": {
          ".validate": "newData.isString() && newData.val() === 'vous'"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "!newData.exists() || newData.hasChildren(['actif'])"
      },
      "conjointe": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com')",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'cindypepe.cp95@gmail.com'",
        "actif": {
          ".validate": "newData.isBoolean()"
        },
        "publieLeTotal": {
          ".validate": "newData.isBoolean()"
        },
        "accordeLe": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000000000"
        },
        "accordePar": {
          ".validate": "newData.isString() && newData.val() === 'conjointe'"
        },
        "$autre": {
          ".validate": false
        },
        ".validate": "!newData.exists() || newData.hasChildren(['actif'])"
      }
    },
    "totauxPrives": {
      "vous": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com')",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'bigot.richard@gmail.com'",
        "$periode": {
          "montant": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000"
          },
          "nombre": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
          },
          "$autre": {
            ".validate": false
          }
        }
      },
      "conjointe": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'bigot.richard@gmail.com' || auth.token.email === 'cindypepe.cp95@gmail.com')",
        ".write": "auth != null && auth.token.email_verified === true && auth.token.email === 'cindypepe.cp95@gmail.com'",
        "$periode": {
          "montant": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000000"
          },
          "nombre": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000"
          },
          "$autre": {
            ".validate": false
          }
        }
      }
    }
  }
}
